<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Dark mode -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="@color/otp_timer_pill_bg" />
            <corners android:radius="40dp" />
        </shape>
    </item>

    <!-- Default -->
    <item>
        <shape>
            <solid android:color="@color/otp_timer_pill_bg" />
            <corners android:radius="40dp" />
        </shape>
    </item>

</selector>